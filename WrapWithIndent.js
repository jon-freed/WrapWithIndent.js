// WrapWithIndent.js
//
// JavaScript for wrapping plain text to a fixed length and also indent based 
// on the first character(s) in each line.
//
// Incidentally, we're Unabashed fan of Hungarian-like notation around here.  
// (Harumph right back at 'cha. )
//
function sWrapWithIndent( sIn, iMaxWidth /*, bBreakLongerTokens*/ ) {

  var sOut;
  
  
  return sOut;
}
